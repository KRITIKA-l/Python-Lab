# PROGRAM 1 :- write a python program which accepts the radius of circle from the user and compute the area.

pi = 22/7
radius = float(input("Enter the radius (in cm) : "))
print("Area of Circle with radius",radius,"cm is",round((pi*radius**2),2),".\n")